卷积神经网络（CNN）的主要操作及其作用
========================================

1. 卷积操作（Convolution）
   - 使用卷积核（滤波器）在输入数据上滑动，计算点积，提取局部特征（如边缘、纹理等）。
   - 作用：自动学习输入数据中的空间局部特征，参数共享减少参数量。

2. 激活函数（Activation Function）
   - 在卷积结果上施加非线性变换，常用 ReLU（f(x) = max(0, x)）。
   - 作用：引入非线性，使网络能够拟合复杂的映射关系；ReLU 还可缓解梯度消失问题。

3. 池化操作（Pooling）
   - 在特征图上进行下采样，常见有最大池化（Max Pooling）和平均池化（Average Pooling）。
   - 作用：降低特征图的空间尺寸，减少计算量；提高模型对位移、缩放的一定不变性，防止过拟合。

4. 批量归一化（Batch Normalization）
   - 对每一批次数据的中间层输出进行归一化（均值为 0、方差为 1），再通过可学习的缩放和平移参数还原。
   - 作用：加速训练收敛，允许使用更大的学习率；减轻对参数初始化的依赖；起到一定的正则化效果。

5. 全连接层（Fully Connected Layer）
   - 将前面层提取的特征展平后，与所有输出神经元相连。
   - 作用：综合全局特征，完成分类或回归等任务的最终预测。

6. Dropout
   - 训练时随机将一定比例的神经元输出置为 0。
   - 作用：防止过拟合，提高模型的泛化能力。

7. 上采样 / 反卷积（Upsampling / Transposed Convolution）
   - 将低分辨率特征图放大到更高分辨率，常用于分割、生成等任务。
   - 作用：恢复空间分辨率，使网络能够输出与输入同尺寸或更大尺寸的结果。

8. 跳跃连接（Skip Connection / Residual Connection）
   - 将某一层的输出直接加到后续层的输出上（如 ResNet）。
   - 作用：缓解深层网络的梯度消失问题，使网络更易训练，同时保留原始特征信息。

总结
----
CNN 通过卷积提取特征、激活函数引入非线性、池化降维压缩信息，再经全连接层完成预测。
批量归一化和 Dropout 等技术用于稳定训练和防止过拟合。
在更复杂的任务中，上采样和跳跃连接进一步扩展了 CNN 的能力。
